<%perl>

&DBDefs::DB_READ_ONLY or return 0;

my ($fHeader, $fFooter, $sMessage) = @_;

$m->comp("/comp/sidebar", title => "Updates temporarily forbidden")
	if $fHeader;
</%perl>

<center>

	<p><% &DBDefs::DB_READ_ONLY_MESSAGE %></p>

</center>

<%perl>
$m->comp("/comp/footer")
	if $fFooter;

return 1;

</%perl>

%# vi: set ts=4 sw=4 ft=mason :