%# vi: set ts=4 sw=4 ft=mason :
<%perl>

my ($url) = @_;

my $host = $r->header_in("Host") || &DBDefs::WEB_SERVER;

$url = "http://" . $host . $url
	unless $url =~ /^\w+:/;
$r->header_out(Location => $url);
$m->abort(303);

</%perl>
