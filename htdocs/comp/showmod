<%args>
$mod
$mode
</%args>

<%perl>

my $voted = $mod->GetVoteId();
my $text = "(Your change)";
if ($voted)
{
   $text = "(Already voted)";
}
my @entered = gmtime($mod->GetExpireTime());
my $time = sprintf "%d-%d %02d:%02d", $entered[4]+1, $entered[3], 
                                      $entered[2], $entered[1];

</%perl>

<tr>
<td bgcolor="#FFBA58" colspan=3><img src="/images/pixel.gif" width="1" height="1" alt="" border=""></td>
</tr>
<tr>
   <td width=38% valign=top bgcolor=white class="body">
     By: <% $mod->GetModeratorName() |n %> 
     (exp: <% $time |n %> id: <% $mod->GetId() %>)
   </td>
   <td width=38% valign=top bgcolor=white class="body">
     <% $mod->ShowModType() |n %> 
   </td>
   <td width=24% align=center valign=top valign=top bgcolor=white class="body">
     <% $mod->GetYesVotes() |n %> yes,
     <% $mod->GetNoVotes() |n %> no
   </td>
</tr>
<tr>
   <td width=40% valign=top bgcolor=white class="body">
     <% $mod->ShowModPrev() |n %> 
   </td>
   <td width=40% valign=top bgcolor=white class="body">
     <% $mod->ShowModNew() |n %> 
   </td>
   <td width=20% align=center valign=top bgcolor=white class="body">
% if ($mode == Moderation::TYPE_MINE) {
     <% $mod->GetChangeName() |n %>
% } elsif ($mode == Moderation::TYPE_VOTED) {
     <% $mod->GetVoteText($voted) |n %>
     (<% $mod->GetChangeName() |n %>)
% } elsif (($session{user} ne $mod->GetModerator() && !$voted) || 
%          DBDefs::ALLOW_SELF_VOTE) {
     Yes <input type="radio" name="rowid<% $mod->GetId() %>" value="yes">
     No <input type="radio" name="rowid<% $mod->GetId() %>" value="no">
     Abst <input type="radio" name="rowid<% $mod->GetId() %>" value="abs" CHECKED>
% } else {
     <% $text %> 
% }
   </td>
</tr>
