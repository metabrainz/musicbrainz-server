<%args>
$mb
$query
$title
</%args>

<%perl>

my ($sth, $result);

$sth = $mb->{DBH}->prepare($query);
if ($sth->execute)
{
   my @row;
 
   @row = $sth->fetchrow_array;
   $result = '<tr><TD valign=top>' . $title . '</TD>';
   $result .= '<TD valign=top>' . $row[0]. '</td></tr>';
}
$sth->finish;
return $result;
</%perl>
