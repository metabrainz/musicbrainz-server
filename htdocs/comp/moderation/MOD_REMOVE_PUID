%# vi: set ts=4 sw=4 ft=mason :

<%method ShowPreviousValue>
% my $this = shift;
% my $new = $this->{'new_unpacked'};

%# TODO show track name.  (We used to show the PUID ID, but that's pretty
%# useless to look at).
% if ($new->{'TrackId'}) {
	Old:
	<a href="/puid/<% $this->GetPrev %>">This PUID</a>
	was removed from
	<a href="/showtrack.html?trackid=<% $new->{'TrackId'} %>">this track</a>
% } else {
	[error]
% }

</%method>

<%method ShowNewValue>
% 	my $this = shift;
</%method>

%# eof
