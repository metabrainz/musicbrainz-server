%# vi: set ts=4 sw=4 ft=mason :
<%perl>

return 1 if $session{user};
my ($fHeader, $fFooter) = @_;

$m->comp("/comp/sidebar", title => "Login Required")
	if $fHeader;
</%perl>

<p>You must be logged in to edit the database.</p>
<& /comp/tablebegin, title=>'Login' &>
<& /comp/loginbox &>
<& /comp/tableend &>

<%perl>
$m->comp("/comp/footer")
	if $fFooter;

return 0;

</%perl>
