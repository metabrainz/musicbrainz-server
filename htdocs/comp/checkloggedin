%# vi: set ts=4 sw=4 ft=mason :
<%perl>

return 1 if $session{uid};
my ($fHeader, $fFooter, $sMessage) = @_;
$sMessage ||= "You must be logged in to edit the database.";

$m->comp("/comp/sidebar", title => "Login Required")
	if $fHeader;
</%perl>

<center>

	<p><% $sMessage %></p>

	<div class="TitledBox" style="width: 20em">
		<div class="TitledBoxTitle">Login</div>
		<& /comp/loginbox &>
	</div>

</center>

<%perl>
$m->comp("/comp/footer")
	if $fFooter;

return 0;

</%perl>
