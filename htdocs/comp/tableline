<%args>
$name
$text
$bold=>0
</%args>

<tr>
   <td valign=top bgcolor=white class="body"><% $name %></td>
   <td valign=top bgcolor=white class="body">
% $m->out('<span class="bold">') if $bold;
% (ref($text) eq "CODE") ? $text->() : $m->out($text);
% $m->out('</span>') if $bold;
   </td>
</tr>
