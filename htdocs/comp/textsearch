<%args>
$default => 'artist'
$limit => 25
$text => ''
</%args>
% $default = lc $default;

<form method="get" action="/newsearch.html" class="formstyle" id="TextSearchForm">

<div style="margin: 0.3em">
  <b>Artist/Album/Track Search</b>
</div>

<center>

  <div class="CenterChildren" style="margin: 0.3em">
    Find 
    <select name="limit">
        <option value="25" <% ($limit eq 25) ? 'selected' : '' %>>up to 25</option>
        <option value="50" <% ($limit eq 50) ? 'selected' : '' %>>up to 50</option>
        <option value="100" <% ($limit eq 100) ? 'selected' : '' %>>up to 100</option>
        <option value="0" <% ($limit eq '0') ? 'selected' : '' %>>all</option>
    </select>
    <select name="table">
        <option value="artist" <% ($default eq 'artist') ? 'selected' : '' %>>artists</option>
        <option value="album" <% ($default eq 'album') ? 'selected' : '' %>>albums</option>
        <option value="track" <% ($default eq 'track') ? 'selected' : '' %>>tracks</option>
    </select>
  </div>

  <div class="CenterChildren" style="margin: 0.3em">
    <input type="text" name="search" value="<% $text %>" size="30">
  </div>

  <div class="CenterChildren" style="margin: 0.3em">
    <input type="submit" value="Search"><br>
  </div>

</center>

</form>
