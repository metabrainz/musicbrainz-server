<td width="10%" align=right>

% if ($ARGS{offset} + $ARGS{num} < $ARGS{rows}) {
    <a href="/<% $ARGS{url} %>?offset=<% $ARGS{offset} + $ARGS{num} %>">
    <% $ARGS{text} %></a>
% } else {
    <% $ARGS{text} %></a>
% }

</td>
