% my $sep = ($ARGS{url} =~ /\?/) ? '&' : '?';

<td width="20%" align=right>

% if ($ARGS{rows} > $ARGS{num}) {
    <a href="/<% $ARGS{url} %><% $sep %>offset=<% $ARGS{offset} + $ARGS{num} %>">
    <% $ARGS{text} %></a>
% } else {
    &nbsp;
% }

</td>
