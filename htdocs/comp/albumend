<%args>
$artist
$album
</%args>

<%perl>

my (@ids, $di, $id);

$di = Diskid->new($album->{DBH});
@ids = $di->GetDiskIdFromAlbum($album->GetId());

if (exists $session{user} && scalar(@ids) > 0) 
{
    if ($album->GetArtist() == Artist::VARTIST_ID) 
    {
       mc_out('<td colspan="4"');
    }
    else
    {
       mc_out('<td colspan="3"');
    }
    mc_out('align="center">');

    foreach $id (@ids)
    {
       mc_out("CD Index Id: $id<br>");
    }

    mc_out("</td></tr>");
}

</%perl>
  </table>
</td></tr>
</table> 
% if (exists $session{user}) {
   <center>
   [ <a href="/addtrack.html?album=<% $album->GetId() %>&artist=<% $artist->GetId() %>">Add a missing track</a> | 
   <a href="/movealbum.html?album=<% $album->GetId() %>&artist=<% $artist->GetId() %>">Move</a> 
%#   <a href="/mergealbum.html?album=<% $album->GetId() %>&artist=<% $artist->GetId() %>">Merge</a> 
%    if ($album->GetArtist() != Artist::VARTIST_ID) {
      | <a href="/tomac.html?album=<% $album->GetId() %>&artist=<% $artist->GetId() %>&track=1">Convert to multiple artists</a>
%    } else {
      | <a href="/tosac.html?album=<% $album->GetId() %>&artist=<% $artist->GetId() %>&track=1">Convert to single artist</a>
%    }
   ]
% }
<p>
