<%args>
$artist
</%args>

<%perl>

my $bgcolor;
$bgcolor = $artist->GetModPending() ? 'bgcolor="#FFFFCC"' : '';

</%perl>

<table>
  <tr>
    <td <% $bgcolor |n %> class="title">
      <font size=+2><% $artist->GetName() %></font>
    </td>
    <td>
% if (exists $session{user} && $artist->GetId() != Artist::VARTIST_ID) {
      (<a href="/editartist.html?id=<% $artist->GetId() %>">Edit</a> |
      <a href="/mergeartist.html?id=<% $artist->GetId() %>">Merge</a>)
% } else {
      &nbsp;
% }
    </td>
  </tr>
</table>
<p>
