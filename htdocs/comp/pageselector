<%perl>

sub PageNumbers
{
    my ($start, $end, $url, $index, $pagesize) = @_;
    my $out = "";

    for my $i ($start .. $end)
    {
        if ($i != $index)
        {
            $out .= "<a href=\"$url&offset=" . ($i * $pagesize) .  "\">" . ($i + 1) .
                    "</a>&nbsp;";
        }
        else
        {
            $out .= "<b>" . ($i + 1) . "</b>&nbsp;";
        }
    }

    return $out;
}

# I used to have an %args block here but Mason kept crashing on it
my $offset = $ARGS{offset};
my $pagesize = $ARGS{pagesize};
my $url = $ARGS{url};
my $numitems = $ARGS{numitems};
my $numlinks = $ARGS{numlinks};
my $snaptoend = $ARGS{snaptoend};

my $index = int($offset / $pagesize);
my $pagecount = int(($numitems+$pagesize-1) / $pagesize);
return undef if ($pagecount <= 1);

my $start = $index - $numlinks;
if ($start < $snaptoend) { $start = 0 }

my $end = $index + $numlinks;
if ($end > $pagecount-1-$snaptoend) { $end = $pagecount-1 }

</%perl>

<table width="100%"><tr>
<td align="center" valign="absmiddle" NOWRAP>
<form method="post" action="<% $url %>" style="padding-top: 0.5em">
	[ Page
	<input type="text" name="page" value="<% $index+1 %>"
		style="
			width: 2em; margin-left: 0.5em; margin-right: 0.5em;
			border: 0;
		">
	of <% $pagecount %> ]
</form>
</td>
<td align="center" valign="center">
% if ($index > 0) {
    <a href="<% $url %>&offset=0"
    	title="first page"
    	>&lt;&lt;</a>&nbsp;
    <a href="<% $url %>&offset=<% ($index - 1) * $pagesize %>"
    	title="previous page"
    	>&lt;</a>&nbsp;
% } else {
    &lt;&lt;
    &lt;
% }

  &nbsp;

% if ($start > 0) {
	...
% }

  <% PageNumbers($start, $end, $url, $index, $pagesize) |n %>

% if ($end < $pagecount-1) {
	...
% }

% if ($index < $pagecount - 1) {
    <a href="<% $url %>&offset=<% ($index + 1) * $pagesize %>"
    	title="next page"
    	>&gt;</a>&nbsp;
    <a href="<% $url %>&offset=<% ($pagecount - 1) * $pagesize %>"
    	title="last page"
    	>&gt;&gt;</a>&nbsp;
% } else {
    &gt;
    &gt;&gt;
% }

</td></tr></table>

