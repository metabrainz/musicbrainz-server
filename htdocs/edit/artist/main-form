<%perl>
	# -----------------------------------------------------------------------------
	#                               Musicbrainz.org
	#                        Copyright (c) 2001 Robert Kaye
	# -----------------------------------------------------------------------------
	#  This software is provided "as is", without warranty of any kind, express or
	#  implied, including  but not limited  to the warranties of  merchantability,
	#  fitness for a particular purpose and noninfringement. In no event shall the
	#  authors or  copyright  holders be  liable for any claim,  damages or  other
	#  liability, whether  in an  action of  contract, tort  or otherwise, arising
	#  from,  out of  or in  connection with  the software or  the  use  or  other
	#  dealings in the software.
	#
	#  GPL - The GNU General Public License    http://www.gnu.org/licenses/gpl.txt
	#  Permits anyone the right to use and modify the software without limitations
	#  as long as proper  credits are given  and the original  and modified source
	#  code are included. Requires  that the final product, software derivate from
	#  the original  source or any  software  utilizing a GPL  component, such  as
	#  this, is also licensed under the GPL license.
	# -----------------------------------------------------------------------------
	#
	# Summary:
	# -----------------------------------------------------------------------------
	# Add/Edit Artist form
	#
	# $Id$
	#
</%perl>
<%args>

	$artist => undef
	$name => ""
	$sortname => ""
	$type => 0
	$begindate => [ ]
	$enddate => [ ]
	$notetext => ""
	$returnurl => ""
	$mbid => ""

	@dupes => ()
	@messages => ()
	$confirm => 0
	$resolution => ""
	$attempts => 0

</%args>
<%perl>

	my $begindate_str = MusicBrainz::Server::Artist::GetBeginDateName($type);
	my $enddate_str = MusicBrainz::Server::Artist::GetEndDateName($type);

</%perl>

% 	if (@messages)
%	{

		<tr>
			<td>&nbsp;</td>
			<td class="error">
				<ul>
					<li>
						<%perl> $m->out(join "</li><li>", @messages), </%perl>
					</li>
				</ul>
			</td>
		</tr>

% 	}

		<tr>
			<td class="label">
				<label for="editartist_type">Type:</label></td>
			<td>
				<select name="type" onchange="artistTypeChanged(this.value);"
					id="editartist_type">
					<& /comp/options,
					[[0, 'Unknown'], [1, 'Person'], [2, 'Group']], $type &>
				</select></td>
		</tr>
		<tr class="spaced">
			<td colspan="2" />
		</tr>

% 	if (defined $artist)
%	{

		<tr>
			<td class="label">
				<label>Current Name:</label></td>
			<td class="field">
				<& /comp/form/currentvalue,
					artist => $artist, display => $artist->GetName(),
					mp_type => &ModDefs::MOD_EDIT_ARTIST &></td>
		</tr>

% 	}

		<tr>
			<td class="label">
				<label for="editartist_name">Artist Name:</label></td>
			<td class="field">
				<input type="text" name="name" class="textfield"
					id="editartist_name" size="50" value="<% $name %>" />
				<& /comp/form/jsbutton, id=>"BTN_ARTIST::name" &></td>
		</tr>
		<tr class="spaced">
			<td colspan="2" />
		</tr>

% 	if (defined $artist)
%	{

		<tr>
			<td class="label">
				<label>Current Sort Name:</label></td>
			<td class="field">
				<& /comp/form/currentvalue,
					artist => $artist, display => $artist->GetSortName(),
					mp_type => &ModDefs::MOD_EDIT_ARTISTSORTNAME &></td>
		</tr>

% 	}

		<tr>
			<td class="label">
				<label for="editartist_sortname">Sort Name:</label></td>
			<td class="field">
				<input  type="text" name="sortname" class="textfield"
					id="editartist_sortname" size="50" value="<% $sortname %>" />
				<& /comp/form/jsbutton, id=>"BTN_ARTIST_SORTGUESS::name::sortname" &>
				<& /comp/form/jsbutton, id=>"BTN_SORTCOPY::name::sortname" &></td>
		</tr>

		<tr>
			<td>&nbsp;</td>
			<td class="formsection">
				Dates are in the format <em>YYYY-MM-DD</em>. Partial dates <br/>
				<em>YYYY-MM</em> or just <em>YYYY</em> are OK, or you can omit the date entirely.</td>
		</tr>

		<tr>
			<td class="label">
				<label for="editartist_begindate"><span id="begindate_text"><% $begindate_str %></span>:</label></td>
			<td class="field">
				<& /comp/dateinput,
					name_prefix => "begindate", value => $begindate,
					id => "editartist_begindate" &>
				Year-Month-Day</td>

		</tr>
		<tr>
			<td class="label">
				<label for="editartist_enddate"><span id="enddate_text"><% $enddate_str %></span>:</label></td>
			<td class="field">
				<& /comp/dateinput,
					name_prefix => 'enddate', value => $enddate,
					id => "editartist_enddate" &>
				Year-Month-Day</td>
		</tr>

%	if (@dupes and ($resolution eq "" or not $confirm))
%	{
%		my $many = (@dupes > 1);

		<tr>
			<td>&nbsp;</td>
			<td class="formsection">
				<div class="similarartist">
					<span class="warning">Warning:</span>
						You are attempting to <% (defined $artist ? "edit" : "add") %>
						the artist <% $name %>, which appears to be very similar,
						if not exactly the same as

%		if ($many)
%		{
						these <span id="AddArtistExistingList">existing artists:
%		}
%		else
%		{
						the <span id="AddArtistExistingList">existing artist called
%		}
						<table style="margin-left: 45px; border-collapse: collapse; border: 0px">

%		for my $i (0..$#dupes)
%		{
							<tr><td>
								<& /comp/linkartist, artist => $dupes[$i] &>
							</td></tr>
%		}
						</table>
						</span>

						If you are certain that you want to <% (defined $artist ? "edit" : "add") %>
						this artist, then:

						<ul style="margin-top: 5px">
							<li>
								You are <strong>required</strong> to enter a comment which allows
								to distinguish this artist from
% 		if ($many)
% 		{
								the other artists with the same name.
% 		}
% 		else
% 		{
								the other artist called <% $dupes[0]->GetName %>.
%		}
							</li>
							<li>
								Confirm by activating the checkbox below that you have read,
								and understood how we distinguish duplicate artists above and
								you know what you are doing.</li>
						</ul>
					</div></td></tr>
%	}

	<tr>
		<td>&nbsp;</td>
		<td class="formsection">
			The comment field is used to differentiate between artists with the same, <br/>
			or confusingly similar, names.</td>
	</tr>

% 	if (defined $artist)
%	{
	<tr>
		<td class="label">
			<label>Current Comment:</label></td>
		<td class="field">
			<div class="oldtextfield"><% $artist->GetResolution() || "(none)" %></div></td>
	</tr>
% 	}

	<tr>
		<td class="label">
			<label for="editartist_resolution">Comment:</label></td>
		<td class="field">
			<input type="text" name="resolution" class="textfield"
				id="editartist_resolution" size="50" maxlength="64"
				value="<% $resolution %>" /></td>
	</tr>

% 	if (@dupes)
%	{

		<tr>
			<td class="label">
				<label for="editartist_confirm">Confirm:</label></td>
			<td>
				<input type="checkbox" id="editartist_confirm"

%				$m->out(qq! checked="checked" !) if ($confirm);

					name="confirm" value="1" />

%		if (defined $artist)
%		{

					I <strong>really</strong> want to edit this artist
					which has the same name as an existing artist.

%		}
%		else
%		{

					I <strong>really</strong> want to create
					this new artist with the same name as
					an existing artist.

%		}

			</td>
		</tr>

%	}

		<tr>
			<td>&nbsp;</td>
			<td class="notetext">
				<& /comp/notetext,
					notetext => $notetext,
				&>

% 	if (defined $artist)
%	{

				<input type="hidden" name="id" value="<% $artist->GetId() %>" />

% 	}

				<input type="hidden" name="attempts" value="<% $attempts + 1 %>" />
				<input type="hidden" name="returnurl" value="<% $returnurl %>" /></td>
				<input type="hidden" name="mbid" value="<% $mbid %>" /></td>
		</tr>

%# vi: set ts=4 sw=4 ft=mason :
