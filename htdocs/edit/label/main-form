<%perl>
	# -----------------------------------------------------------------------------
	#                               Musicbrainz.org
	#                        Copyright (c) 2001 Robert Kaye
	# -----------------------------------------------------------------------------
	#  This software is provided "as is", without warranty of any kind, express or
	#  implied, including  but not limited  to the warranties of  merchantability,
	#  fitness for a particular purpose and noninfringement. In no event shall the
	#  authors or  copyright  holders be  liable for any claim,  damages or  other
	#  liability, whether  in an  action of  contract, tort  or otherwise, arising
	#  from,  out of  or in  connection with  the software or  the  use  or  other
	#  dealings in the software.
	#
	#  GPL - The GNU General Public License    http://www.gnu.org/licenses/gpl.txt
	#  Permits anyone the right to use and modify the software without limitations
	#  as long as proper  credits are given  and the original  and modified source
	#  code are included. Requires  that the final product, software derivate from
	#  the original  source or any  software  utilizing a GPL  component, such  as
	#  this, is also licensed under the GPL license.
	# -----------------------------------------------------------------------------
	#
	# Summary:
	# -----------------------------------------------------------------------------
	# Add/Edit Label form
	#
	# $Id$
	#
</%perl>
<%args>

	$label => undef
	$name => ""
	$sortname => ""
	$labelcode => ""
	$type => 0
	$country => undef
	$begindate => [ ]
	$enddate => [ ]
	$notetext => ""
	$returnurl => ""

	@dupes => ()
	@messages => ()
	$confirm => 0
	$resolution => ""

</%args>
<%perl>

	my $countries_menu = $m->comp("../albumreleases/countries-menu.inc");
	my $countries_menu_2 = [ [ undef, "[select a country]" ], @$countries_menu ];

</%perl>

% 	if (@messages)
%	{
		<tr>
			<td>&nbsp;</td>
			<td class="error">
				<ul>
					<li>
						<%perl> $m->out(join "</li><li>", @messages), </%perl>
					</li>
				</ul>
			</td>
		</tr>
% 	}

		<tr>
			<td class="label">
				<label for="editartist_type">Type:</label></td>
			<td>
				<select name="type" id="editartist_type">
					<& /comp/options, MusicBrainz::Server::Label::GetLabelTypes(), $type &>
				</select></td>
		</tr>

		<tr>
			<td class="label">
				<label for="editlabel_name">Label Name:</label></td>
			<td class="field">
				<input type="text" name="name" class="textfield"
					id="editlabel_name" size="50" value="<% $name %>" />
				<& /comp/form/jsbutton, id=>"BTN_ARTIST::name" &></td>
		</tr>

		<tr>
			<td class="label">
				<label for="editartist_sortname">Sort Name:</label></td>
			<td class="field">
				<input  type="text" name="sortname" class="textfield"
					id="editartist_sortname" size="50" value="<% $sortname %>" />
				<& /comp/form/jsbutton, id=>"BTN_LABEL_SORTGUESS::name::sortname" &>
				<& /comp/form/jsbutton, id=>"BTN_SORTCOPY::name::sortname" &></td>
		</tr>

		<tr>
			<td class="label">
				<label for="editlabel_labelcode">Label Code:</label></td>
			<td class="field">
				<input  type="text" name="labelcode" class="textfield""
					id="editlabel_labelcode" size="5" value="<% $labelcode %>" /></td>
		</tr>

		<tr>
			<td class="label">
				<label for="editlabel_country">Country:</label></td>
			<td class="field">
				<select name="country" id="editlabel_country">
					<& /comp/options, $countries_menu_2, $country &>
					</select></td>
		</tr>

		<tr>
			<td>&nbsp;</td>
			<td class="formsection">
				Dates are in the format <em>YYYY-MM-DD</em>. Partial dates <br/>
				<em>YYYY-MM</em> or just <em>YYYY</em> are OK, or you can omit the date entirely.</td>
		</tr>

		<tr>
			<td class="label">
				<label for="editlabel_begindate">Begin Date:</label></td>
			<td class="field">
				<& /comp/dateinput,
					name_prefix => "begindate", value => $begindate,
					id => "editlabel_begindate" &>
				Year-Month-Day</td>

		</tr>
		<tr>
			<td class="label">
				<label for="editlabel_enddate">End Date:</label></td>
			<td class="field">
				<& /comp/dateinput,
					name_prefix => 'enddate', value => $enddate,
					id => "editlabel_enddate" &>
				Year-Month-Day</td>
		</tr>

%	if (@dupes and ($resolution eq "" or not $confirm))
%	{
%		my $many = (@dupes > 1);

		<tr>
			<td>&nbsp;</td>
			<td class="formsection">
				<div class="similarartist">
					<span class="warning">Warning:</span>
						You are attempting to <% (defined $label ? "edit" : "add") %>
						the label <% $name %>, which appears to be very similar,
						if not exactly the same as

%		if ($many)
%		{
						these <span id="AddLabelExistingList">existing labels:
%		}
%		else
%		{
						the <span id="AddLabelExistingList">existing label called
%		}
						<table style="margin-left: 45px; border-collapse: collapse; border: 0px">

%		for my $i (0..$#dupes)
%		{
							<tr><td>
								<& /comp/linklabel, label => $dupes[$i] &>
							</td></tr>
%		}
						</table>
						</span>

						If you are certain that you want to <% (defined $label ? "edit" : "add") %>
						this label, then:

						<ul style="margin-top: 5px">
							<li>
								You are <strong>required</strong> to enter a comment which allows
								to distinguish this label from
% 		if ($many)
% 		{
								the other labels with the same name
% 		}
% 		else
% 		{
								the other label called <% $dupes[0]->GetName %>.
%		}
							.</li>
							<li>
								Confirm by activating the checkbox below that you have read,
								and understood how we distinguish duplicate labels above and
								you know what you are doing.</li>
						</ul>
					</div></td></tr>
%	}

	<tr>
		<td>&nbsp;</td>
		<td class="formsection">
			The comment field is used to differentiate between labels with the same, <br/>
			or confusingly similar, names.</td>
	</tr>

% 	if (defined $label)
%	{
	<tr>
		<td class="label">
			<label>Current Comment:</label></td>
		<td class="field">
			<div class="oldtextfield"><% $label->GetResolution() || "(none)" %></div></td>
	</tr>
% 	}

	<tr>
		<td class="label">
			<label for="editlabel_resolution">Comment:</label></td>
		<td class="field">
			<input type="text" name="resolution" class="textfield"
				id="editlabel_resolution" size="50" maxlength="64"
				value="<% $resolution %>" /></td>
	</tr>

% 	if (@dupes)
%	{

		<tr>
			<td class="label">
				<label for="editlabel_confirm">Confirm:</label></td>
			<td>
				<input type="checkbox" id="editlabel_confirm"

%				$m->out(qq! checked="checked" !) if ($confirm);

					name="confirm" value="1" />

%		if (defined $label)
%		{

					I <strong>really</strong> want to edit this label
					which has the same name as an existing label.

%		}
%		else
%		{

					I <strong>really</strong> want to create
					this new label with the same name as
					an existing label.

%		}

			</td>
		</tr>

%	}

		<tr>
			<td>&nbsp;</td>
			<td class="notetext">
				<& /comp/notetext,
					notetext => $notetext,
				&>

% 	if (defined $label)
%	{

				<input type="hidden" name="id" value="<% $label->GetId() %>" />

% 	}

				<input type="hidden" name="returnurl" value="<% $returnurl %>" /></td>
		</tr>

%# vi: set ts=4 sw=4 ft=mason :
