[% FOREACH item=items %]
    [% IF item.${group_var} != group_last %]
        [% PROCESS group_foot IF !loop.first %]
        [% PROCESS group_head %]
    [% END %]
    [% PROCESS group_item %]
    [% group_last = item.$group_var %]
[% END %]
[% PROCESS group_foot IF items.size > 0 %]
