rules:

  # Stylistic Issues
  max-len: [warn, {code: 78,
                   ignoreUrls: true,
                   ignoreStrings: true,
                   ignoreTemplateLiterals: false,
                   ignoreRegExpLiterals: true}]

